📚 What We’ll Learn

🖼️ 1. Image Processing Basics

* What is an image?
* Pixels and pixel values
* Image dimensions and coordinates
* Image channels
* RGB, BGR and Grayscale
* Understanding how images are represented digitally

🐍 2. OpenCV with Python

* What is OpenCV?
* Importing OpenCV using cv2
* Reading images with cv2.imread()
* Displaying images with cv2.imshow()
* Using cv2.waitKey()
* Closing windows with cv2.destroyAllWindows()
* Saving images with cv2.imwrite()

🔧 3. Basic Image Operations

* Resizing images with cv2.resize()
* Cropping images
* Region of Interest (ROI)
* Basic image-processing operations

🎥 4. Video & Webcam

* What is a video?
* What is a frame?
* Video as a sequence of frames
* Frame-by-frame processing
* cv2.VideoCapture()
* Using VideoCapture(0) for a webcam
* cap.isOpened()
* cap.read()
* Understanding ret and frame
* Releasing the camera with cap.release()

🎨 5. Color Spaces

* BGR vs RGB
* Grayscale
* cv2.cvtColor()
* Introduction to HSV

🎭 6. Masking & Color Detection

* What is a mask?
* Binary masks
* HSV-based color selection
* cv2.inRange()
* Applying masks to images
* Basic live color-mask activity
